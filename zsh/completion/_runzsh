#compdef runzsh

_runzsh() {
    local curcontext="$curcontext" state line
    typeset -A opt_args

    _arguments -C '(-): :->user' && return

    case $state in
        (user) _users ;;
    esac
}

_runzsh "$@"
